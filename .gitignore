# Binaries
/devicedeck
/dist/
/bin/

# Go
coverage.out
coverage.html
*.test

# Swift sidecar
.build/
*.xcodeproj

# macOS
.DS_Store

# Local-only Claude state
.claude/settings.local.json

# Private strategy / planning docs — local only, never committed
PROJECT-BRIEF.md
PLAN-*.md
RESEARCH-*.md
research/
docs_local/
ROADMAP.md
LAUNCH-PLAN.md

# Internal AI / dev config — kept local (functional), never published
CLAUDE.md
.claude/
scripts/README.md

# Local scratch — runner build cache, replay-gate reports, Playwright dumps
/cache/
/reports/
/test-results/
.playwright-mcp/
/android-login.yaml
/android-login.guard.json

# Signing material — lives only in GitHub Secrets, never in the repo
*.p12
*.p8
*.pem
*.cer
*.mobileprovision
AuthKey_*
.env
.env.*

# JS lint / type-check tooling (package.json at the root)
/node_modules/
